# Notes

The `.gitkeep` file is retained only to make the item directory visible before generated JSON certificates are committed. Generated audit JSON files are produced by the workflow and will be archived only after review.
