# Item 6 certification progress — 2026-07-26

## Scope and theorem status

This record fixes the validated state at branch head

`d7f08c9507a028c303601a69901880a14d5c1f49`.

The final target is

\[
\Psi_\lambda(w)=A_\lambda'(w)>0
\qquad(\lambda\ge 1,\ 0<w<1).
\]

Item 6 as a whole remains **NOT CERTIFIED**. This file records one newly certified numerical node and the current bounded tail diagnostic. It does not promote incomplete diagnostics to proof nodes.

## `P-MODULUS` — CERTIFIED

GitHub Actions run: `30192590535`  
Artifact: `prolate-item6-pole-modulus-split`  
Artifact ID: `8629100350`  
Artifact digest: `sha256:0f4a6a73c16b8009ad4804076e5d237446f5f1a9944d0bf495ff17494a90f92e`

Certified statement:

> `|A_lambda''(w)|` is bounded by the certified split weighted constant on `1<=lambda<=100`, `63/64<=w<1`, and `Psi_lambda(1-u)>0` for `0<=u<=2^-24` using `Phi>=43/5000`.

Certified constants:

- boundary floor: `43/5000 = 0.0086`;
- exact endpoint scale: `epsilon=2^-24=1/16777216`;
- absolute safe ceiling: `90177536/625 = 144284.0576`;
- weighted certified bound:
  `[143700.39247282134784787450023088319074824270803725 +/- 8.47e-45]`;
- remaining absolute margin:
  `[583.66512717865215212549976911680925175729196275 +/- 8.85e-45]`;
- certified modulus loss:
  `[0.008565210847426733246318966164045524045720261814431 +/- 6.00e-52]`;
- residual uniform far ceiling:
  `[144317.91458478051661851741288647364566403902922025 +/- 3.84e-45]`.

All five exact regions are certified with terminal zero:

| Region | Evaluations | Certified leaves | Terminal boxes | Maximum frontier |
|---|---:|---:|---:|---:|
| inner `d=u^2 y^2` | 326872 | 163440 | 0 | 26 |
| outer near `u=r sqrt(2)t` | 146356 | 73182 | 0 | 22 |
| junction signed chart `[1/128,1/64]` | 57440 | 28724 | 0 | 19 |
| middle factored chart `[1/64,255/256]` | 996728 | 498368 | 0 | 25 |
| endpoint signed chart `[255/256,1]` | 87356 | 43682 | 0 | 23 |
| **Total** | **1614752** | **807396** | **0** | — |

The exact split audit, endpoint factorization audit, signed-angle audit, exact rational coverage checks, and all final inequalities passed. In particular, the factor `z^2-2` is factored from the complete transformed density and is not cancelled by division.

Script hash manifest from the certificate:

- endpoint wrapper: `4214f0224d5206adf8b52b18abb3f4aca96e307c3aeca0a97b27f1f413407910`;
- DFS driver: `4977baf99b0f5382ac02d0e70a7f18f285b5c3cb8cebaaa7c721fe33e374515f`;
- base driver: `968aaa8e86250ec1c04a945fa7a7f85e8ad6778e9ee6ef3acb9a2ab6ec0fbea5`.

## Tail interface bounded diagnostic — INCOMPLETE

GitHub Actions run: `30192590525`  
Artifact: `prolate-item6-tail-H-first-six-smoke-summary`  
Artifact ID: `8629119615`  
Artifact digest: `sha256:a3a79c0b2956478724bde63cfce46e45f3a975d62e2fa411bda7f8bf1a618206`

The diagnostic evaluates the equivalent direct quantity

\[
H(\mu,w)>0\iff \Psi_{1/\mu}(w)>0
\qquad(\mu>0,\ w>0),
\]

before division by `mu*w`. It uses the signed-angle kernel, exact removal of the constant `pi^2/4` term, moving-layer charts, and endpoint-square caps.

The tested slab is

\[
\frac1{200}\le\mu\le\frac3{400},
\qquad
\frac1{20}\le w\le\frac34,
\]

split into six `w` bands. Every band completed as a bounded diagnostic, but none is a certificate:

- status: `INCOMPLETE` for all six bands;
- evaluations: `8` per band;
- certified leaves: `0` per band;
- terminal boxes: `9` per band;
- terminal reason: `max_boxes_exhausted` only;
- exact rational area invariant: true for all six bands;
- no certified sign counterexample was produced.

After the endpoint and cancellation repairs, many refined component integrals are finite with imaginary part containing zero. Larger parameter boxes still produce nonfinite component enclosures or intervals crossing zero. The remaining issue is interval dependency and chart width, not a certified negative value.

This diagnostic must not be used as `T-INTERFACE` in the final DAG.

## Exact and implementation repairs recorded in the branch

- rational interval constructors are audited under `python-flint==0.9.0`;
- the unsupported/ambiguous two-argument `fmpq` construction was replaced by string midpoint-radius construction;
- the `C=1` hypergeometric endpoint failure in `regular_hbar` was isolated;
- endpoint-square caps remove the square-root endpoint singularity from the integration parameter;
- exact correlations such as `c-w=mu(8t-4)` and `P=mu^2 P_tilde` are preserved where available;
- direct `Psi` is used for compact-tail sign testing to avoid artificial `1/(mu*w)` interval amplification;
- retired workflow entry points were removed to reduce runner contention; proof sources and prior artifacts remain preserved.

## Remaining proof nodes

1. `F-CENTER`, `F-MIDDLE`, and `F-POLE`: certified combined finite-grid outputs;
2. `P-DYADIC`: certified signed dyadic-pole output;
3. `T-INTERFACE`: terminal-zero compact-tail positivity certificate;
4. `T-MONO`: `M>0` or a rigorously equivalent monotone tail mechanism;
5. `T-INTERIOR-0`: uniform extension to `mu=0`;
6. `T-CENTER` and `T-POLE`: certified center and pole tail overlaps;
7. final exact DAG assembly with no gaps, no overlaps, exact endpoints, and terminal zero.

Binary diagnostic ZIP files are retained as GitHub Actions artifacts rather than committed to the repository. Their artifact IDs and SHA-256 digests above provide the provenance links.